import React from 'react';
import { storiesOf } from '@storybook/react';
import { action } from '@storybook/addon-actions';
import Wrapper from './Wrapper';

storiesOf('React NPS', module).add('default', () => <Wrapper />);
